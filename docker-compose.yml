services:
  backend:
    build:
      context: ./backend
    env_file:
      - ./backend/.env
    ports:
      - "8080:8080"

  frontend:
    build:
      context: ./frontend/web
    env_file:
      - ./frontend/web/.env
    ports:
      - "80:80"
    depends_on:
      - backend
