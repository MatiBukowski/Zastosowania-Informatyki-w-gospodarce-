import RestaurantMenuView from '@/ui/views/restaurants/RestaurantMenuView';
import { ScreenLayout } from '@/ui/layouts/ScreenLayout';

export default function MenuScreen() {
  return (
    <ScreenLayout>
      <RestaurantMenuView />
    </ScreenLayout>
  );
}